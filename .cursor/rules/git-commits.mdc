---
description: Git commit conventions - no trailers
alwaysApply: true
---

# Git commits

- Use plain commit messages only: `git commit -m "message"`
- Never add `--trailer` or any trailer flags to commit commands
- Keep commit messages clear and concise
