# After any "bundle update" or Gemfile change, run:
#   bundle lock --add-platform ruby --add-platform x86_64-linux
# so GitHub Pages (Linux) can build. Or use: ./scripts/bundle-update-safe.sh
source "https://rubygems.org"

gem "github-pages"
